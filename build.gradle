subprojects {
	apply plugin: 'java'
	apply plugin: 'eclipse'
	apply plugin: 'groovy'
	apply plugin: 'idea'

	repositories {
		mavenCentral()
	}

	version = '0.1.1'

	jar { manifest.attributes provider: 'wallerlab.org' }
}
